const assert = require('assert');
const { word } = require('../lib/words');



describe('word', () => {
    newWord= [];
    it('only keeps words with Y and X', () => {
        const word= words('Yeti', 'Green');
        assert.equal('Yeti');
    });
    it('word to phrase', () => {
    
      
    });
});
